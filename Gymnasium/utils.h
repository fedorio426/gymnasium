// utils.h

#include <string>
#include <vector>
#include <cstdarg>

#include <nana/gui.hpp>

using namespace  std;

std::string sformat(const std::string & fmt_str, ...);

std::string to_local(std::string str);
